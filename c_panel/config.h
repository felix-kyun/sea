#define PLUGIN_LEFT \
    X(auto_kill)    \
    X(media)

#define PLUGIN_CENTER \
    X(date_time)

#define PLUGIN_RIGHT \
    X(cpu_temp)      \
    X(ram)           \
    X(cpu)           \
    X(net_speed)     \
    X(battery)

#define SPAWN_COMMAND "sea c_panel"
