#define LEFT \
    X(media)

#define CENTER \
    X(date_time)

#define RIGHT    \
    X(cpu_temp)  \
    X(ram)       \
    X(cpu)       \
    X(net_speed) \
    X(battery)

#define SPAWN_COMMAND "sea c_panel"
