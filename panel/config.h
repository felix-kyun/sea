#define LEFT \
    X(media)

#define CENTER \
    X(date_time)

#define RIGHT     \
    X(net_speed)  \
    X(brightness) \
    X(cpu_temp)   \
    X(ram)        \
    X(cpu)        \
    X(battery)

#define SPAWN_COMMAND "sea c_panel"
